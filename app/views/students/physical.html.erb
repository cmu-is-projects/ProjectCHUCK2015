<center>
    <h1>Physical</h1>
    <h3><%= @student.proper_name %></h3>
    <iframe src="<%= @student.physical_url %>" width="100%" height="400px"></iframe>
    <div>
        <%= link_to "Checkoff", physical_checkoff_student_path(@student) %>
        <%= link_to "Deny", physical_deny_student_path(@student) %>
    </div>
</center>
